import { setupTestDB } from "../../lib/testSetup";

describe("new User()", () => {
  it("should properly assign properties", () => {
    // const user = new user
    expect(true).toEqual(true);
  });
});
