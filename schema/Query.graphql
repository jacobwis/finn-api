type Query {
  currentUser: User
  findBookByID(id: String): Book
  readingList: [Book]
  search(query: String, options: SearchOptions): [Book]
}

input SearchOptions {
  startIndex: Int
  maxResults: Int
}

type SearchResult {
  items: [Book]
  nextPage: Int
}
